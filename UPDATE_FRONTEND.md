# تحديث الفرونت إيند للاتصال بالباك إيند على Render

بعد نشر الباك إيند على Render، اتبع هذه الخطوات لربط الفرونت إيند:

## 1️⃣ احصل على رابط الباك إيند

بعد نشر الباك إيند على Render، ستحصل على رابط مثل:
```
https://intlakaa-backend.onrender.com
```

## 2️⃣ حدّث Environment Variables في Vercel

### الطريقة الأولى: من لوحة تحكم Vercel

1. اذهب إلى مشروع الفرونت إيند على Vercel
2. اضغط على **Settings**
3. اضغط على **Environment Variables**
4. أضف أو حدّث المتغير التالي:

| Name | Value |
|------|-------|
| `VITE_API_URL` | `https://intlakaa-backend.onrender.com` |

5. احفظ التغييرات

### الطريقة الثانية: من ملف `.env` المحلي

إذا كنت تريد تحديث الملف المحلي أيضاً:

```env
# في ملف frontend/.env
VITE_API_URL=https://intlakaa-backend.onrender.com
```

## 3️⃣ أعد نشر الفرونت إيند

### من لوحة تحكم Vercel:

1. اذهب إلى تبويب **Deployments**
2. اضغط على النقاط الثلاث (...) بجانب آخر نشر
3. اختر **Redeploy**
4. اختر **Use existing Build Cache** (أسرع)
5. اضغط **Redeploy**

### من Git:

```bash
# في مجلد الفرونت إيند
git add .
git commit -m "Update API URL to Render backend"
git push origin main
```

Vercel سيقوم بإعادة النشر تلقائياً.

## 4️⃣ اختبر الاتصال

1. افتح موقع الفرونت إيند على Vercel
2. جرب تسجيل الدخول
3. تحقق من أن جميع الطلبات تعمل بشكل صحيح

## 5️⃣ تحديث CORS في الباك إيند

تأكد من أن رابط Vercel مضاف في Environment Variables للباك إيند:

في Render:
1. اذهب إلى الخدمة (Service)
2. **Environment** → **Environment Variables**
3. تأكد من وجود:
   ```
   FRONTEND_URL=https://your-frontend.vercel.app
   ```
4. احفظ التغييرات (سيتم إعادة نشر الخدمة تلقائياً)

## ✅ تم!

الآن الفرونت إيند على Vercel متصل بالباك إيند على Render!

---

## 🔧 استكشاف الأخطاء

### CORS Error في المتصفح؟

```
Access to fetch at 'https://intlakaa-backend.onrender.com/api/...' 
from origin 'https://your-frontend.vercel.app' has been blocked by CORS policy
```

**الحل:**
1. تأكد من أن `FRONTEND_URL` في Render يطابق رابط Vercel بالضبط
2. لا تضع `/` في نهاية الرابط
3. أعد نشر الباك إيند بعد التحديث

### الطلبات بطيئة جداً؟

**السبب:** الخطة المجانية في Render تتوقف الخدمة بعد 15 دقيقة من عدم النشاط.

**الحل:**
- أول طلب بعد التوقف يستغرق 30-60 ثانية
- استخدم خدمة مثل [UptimeRobot](https://uptimerobot.com) لإبقاء الخدمة نشطة
- أو قم بالترقية إلى خطة مدفوعة

### خطأ 404 عند الطلبات؟

**تحقق من:**
1. أن `VITE_API_URL` لا يحتوي على `/` في النهاية
2. أن الطلبات في الفرونت إيند تستخدم المسارات الصحيحة
3. مثال صحيح:
   ```javascript
   const API_URL = import.meta.env.VITE_API_URL; // https://backend.onrender.com
   const response = await fetch(`${API_URL}/api/auth/login`); // ✅
   ```

### خطأ في Authentication؟

**تحقق من:**
1. أن JWT_SECRET في Render يطابق ما تم استخدامه في التطوير
2. أن التوكن يتم إرساله بشكل صحيح في الـ headers
3. مثال:
   ```javascript
   headers: {
     'Authorization': `Bearer ${token}`,
     'Content-Type': 'application/json'
   }
   ```

---

## 📝 ملاحظات مهمة

1. **البيئة (Environment):** تأكد من أن `NODE_ENV=production` في Render
2. **الأمان:** لا تشارك `JWT_SECRET` أو أي معلومات حساسة
3. **المراقبة:** راقب Logs في Render لاكتشاف أي مشاكل
4. **النسخ الاحتياطي:** احتفظ بنسخة من Environment Variables

---

تاريخ الإنشاء: 2026-01-02
